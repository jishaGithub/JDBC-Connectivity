<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.laba.db.mybatis.mapper.VehicleHasVehicleFeatureMapper">

    <insert id="insertVehicleHasVehicleFeature">
        INSERT INTO vehicle_has_vehicle_feature (vehicle_id, vehicle_feature_id)
        VALUES (#{vehicleId}, #{vehicleFeatureId})
    </insert>

    <delete id="deleteVehicleHasVehicleFeature">
        DELETE FROM vehicle_has_vehicle_feature
        WHERE vehicle_id = #{vehicleId} AND vehicle_feature_id = #{vehicleFeatureId}
    </delete>

    <select id="getVehicleFeaturesByVehicleId" resultType="com.solvd.laba.db.model.VehicleHasVehicleFeature">
        SELECT vehicle_id, vehicle_feature_id
        FROM vehicle_has_vehicle_feature
        WHERE vehicle_id = #{vehicleId}
    </select>

    <select id="getVehiclesByFeatureId" resultType="com.solvd.laba.db.model.VehicleHasVehicleFeature">
        SELECT vehicle_id, vehicle_feature_id
        FROM vehicle_has_vehicle_feature
        WHERE vehicle_feature_id = #{vehicleFeatureId}
    </select>

</mapper>
