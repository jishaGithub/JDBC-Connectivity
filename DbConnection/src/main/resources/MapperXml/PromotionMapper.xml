<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.laba.db.mybatis.mapper.PromotionMapper">

    <resultMap id="PromotionResultMap" type="com.solvd.laba.db.model.Promotion">
        <id property="id" column="id"/>
        <result property="promotionName" column="promotion_name"/>
        <result property="discount" column="discount"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
    </resultMap>

    <select id="getPromotionById" resultMap="PromotionResultMap">
        SELECT id, promotion_name, discount, start_date, end_date
        FROM promotion
        WHERE id = #{id}
    </select>

    <select id="getAllPromotions" resultMap="PromotionResultMap">
        SELECT id, promotion_name, discount, start_date, end_date
        FROM promotion
    </select>

    <insert id="insertPromotion">
        INSERT INTO promotion (id, promotion_name, discount, start_date, end_date)
        VALUES (#{id}, #{promotionName}, #{discount}, #{startDate}, #{endDate})
    </insert>

    <update id="updatePromotion">
        UPDATE promotion
        SET promotion_name = #{promotionName},
        discount = #{discount},
        start_date = #{startDate},
        end_date = #{endDate}
        WHERE id = #{id}
    </update>

    <delete id="deletePromotion">
        DELETE FROM promotion
        WHERE id = #{id}
    </delete>

</mapper>
